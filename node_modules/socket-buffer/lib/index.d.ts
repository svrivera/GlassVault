/// <reference types="node" />
/// <reference types="node" />
/// <reference types="node" />
import { Socket } from "net";
import EventEmitter from "events";
export default class SocketBuffer extends EventEmitter {
    private socket;
    private buffer;
    constructor(socket: Socket);
    read(length: number, timeout?: number): Promise<Buffer>;
    until(value: number | number[], include?: boolean, timeout?: number): Promise<Buffer>;
    any(timeout?: number, ignoreBuffer?: boolean): Promise<void>;
    private handleData;
    private handleError;
    private cut;
}
//# sourceMappingURL=index.d.ts.map